name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.11"
      # Keep optional runtime integrations out of static analysis. In
      # particular, Streamlit pulls in NumPy stubs that use Python 3.12-only
      # type-statement syntax while this project intentionally targets 3.11.
      - run: uv sync --group lint
      - run: uv run ruff check src tests
      - run: uv run mypy src

  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: uv sync --all-extras --group test
      - run: uv run pytest -v
